#arrays
#to print array from n to m number
arr1<-c(1:64)#this will print array from 1 to 55
arr1
#we can give shapr to an array using following function
arr2<-c(arr1,dim=c(4,5,6))
arr2
dim(arr2)